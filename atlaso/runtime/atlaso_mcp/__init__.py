"""atlaso_mcp — the Atlaso memory MCP server (the `memory` server).

Cross-tool: the same stdio server plugs into Claude Code, Claude Desktop, Codex,
Cursor, etc. Backed by the shared thin client; no engine inside.
"""
__version__ = "0.1.0"
